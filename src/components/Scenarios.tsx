import { Card, CardHeader, CardTitle, CardDescription } from '@/components/ui/card';

export function Scenarios() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Scénarios branchés</CardTitle>
        <CardDescription>à venir</CardDescription>
      </CardHeader>
    </Card>
  );
}
